# Math module

This directory owns internal vector, matrix, scalar, and SIMD primitives. The
initial implementation should remain scalar and deterministic; optimized paths
must preserve the same observable rasterization rules.
